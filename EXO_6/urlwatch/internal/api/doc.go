// Package api expose le service via HTTP : routage, handlers, middleware
// (logging structuré, recovery), et les DTO JSON (requêtes/réponses) conformes
// au contrat de l'API. Il traduit les erreurs du domaine en codes HTTP.
package api
